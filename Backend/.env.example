# Database
DATABASE_URL=postgresql://user:password@host:5432/dbname

# JWT
JWT_SECRET=your_jwt_secret

# SMTP (optional)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=username
SMTP_PASS=password
SMTP_SECURE=false
FROM_EMAIL=no-reply@example.com

# Other
NODE_ENV=production
PORT=3000

# NOTE: Vercel requires you to add env vars in Project Settings -> Environment Variables
